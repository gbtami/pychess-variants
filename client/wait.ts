import { h } from 'snabbdom/h';
import { VNode } from 'snabbdom/vnode';

import { PyChessModel } from './main';

export function waitView(model: PyChessModel): VNode {
    // TODO stub
    return h('div');
}
